# UBERGO_B2 Environment Configuration
# Copy this to .env and customize as needed

# Database Configuration
DATABASE_URL=postgresql+asyncpg://postgres:%232005REnu@localhost:5432/ubergo

# App Configuration (these can be passed as env vars to individual instances)
# USER_ID and DRIVER_ID are set by the launcher apps
# PORT is also set dynamically

# Optional: Set to False to disable auto-reload in development
# DEBUG=True
